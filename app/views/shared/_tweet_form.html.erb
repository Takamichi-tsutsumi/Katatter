<%= form_for(@tweet) do |f| %>
<%= render 'shared/error_messages', object: f.object %>
<div class="field">
  <%= f.text_area :tubuyaki, placeholder: "今何してる？" %>
</div>
  <%= f.submit "つぶやく", class: "btn btn-large btn-primary"%>
<% end %>
